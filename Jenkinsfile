#!groovy
@library('jenkins-devops') _
def tools = new org.devops.tools()
Hello ()
pipeline {
   agent any
   stages {
      stage('Hello') {
         steps {
            echo 'Hello World'
            //tools.PrintMes （ "this is my pipeline"）
         }
      }
   }
}